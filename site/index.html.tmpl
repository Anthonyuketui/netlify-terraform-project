<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>${brand} — Terraform + Netlify Demo</title>
  <style>
    body {
      font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      margin: 0; padding: 0; min-height: 100vh;
      display: grid; place-items: center; background: #000; color: #f1f1f1;
    }
    .card {
      background: #111; border-radius: 16px; border: 2px solid #d4af37;
      padding: 2.25rem; max-width: 680px; box-shadow: 0 12px 30px rgba(0,0,0,.6);
      text-align: center;
    }
    h1 { color: #d4af37; margin: 0 0 .5rem 0; font-size: 2.25rem; letter-spacing: .5px; }
    p  { color: #ddd; line-height: 1.6; margin: .35rem 0; }
    .btn {
      display: inline-block; margin-top: 1.2rem; padding: .75rem 1.5rem;
      border-radius: .6rem; background: #d4af37; color: #000; font-weight: 700;
      text-decoration: none; transition: transform .2s ease;
    }
    .btn:hover { transform: translateY(-2px); }
    .subtle { color: #aaa; font-size: .95rem; }
  </style>
</head>
<body>
  <main class="card">
    <h1>${brand}</h1>
    <p class="subtle">Discipline ⚡ Automation ⚡ Results</p>

    <p><strong>🏋️ Fitness focus today:</strong> ${motto}</p>
    <p><strong>Fitness × DevOps</strong> — built by discipline, scaled by automation.</p>

    <a class="btn" href="#" target="_blank" rel="noopener">Join the Movement</a>

    <p class="subtle" style="margin-top:1rem;">
      This site was deployed using Terraform + Netlify.<br/>
      Brand & motto randomized on each apply.
    </p>
  </main>
</body>
</html>
